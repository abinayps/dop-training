##Questions

1. Create a struct named Student with fields: 
   - Name (string)
   - Age (int)
   - Grade (string)

 Write a function that takes a Student struct as an argument and prints the student's details.

2. Define a struct Book with fields:
   - Title (string)
   - Author (string)
   - Price (float64)

   Write a method Display() that prints the book details. Create a Book and call the method.

3. Create a struct called Rectangle with:Width (float64)
   - Width (float64)
   - Height (float64)

    Add a method Area() float64 that returns the area. Write a main program to create a Rectangle and print the area.

4. Define a struct Movie with:
   - Title (string)
   - Year (int)

   Create a slice of Movie structs containing at least 3 movies. Print all movie titles released after 2010

5. Define two structs:

Address:
   - City (string)
   - Zip (int)

Person:   - 
   - Name (string)
   - Address (Address)

    Create a Person with an Address and print both.


---------------------------------------------------------------------------

Design a Go-based Employee Management System using structs (Employee, Department, Project,AttendanceRecord) that incorporates multiple datatype and supports operations such as adding, updating, removing employees, assigning departments, searching, filtering.