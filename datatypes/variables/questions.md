1. Write a function that returns a user's first name, last name, age, city, and email as multiple return values.

2. Write a function that validates a user's age and returns a boolean and an error if the age is invalid. (e.g less than 0 or greater than 120).

3. Create a function that returns a user's full name along with the total number of characters in it.

4. Write a function that converts the user’s age from int to string and returns both values.

