Questions:

1) Build an API that takes list of employee names in an office and returns the list of employees sorted in alphabetical order.

2) Build an API that takes an employee name as search term. Check for the existence of this employee in the fixed list of some ten employees and output the appropriate message as response.

3) Build an API that takes a list of feedback ratings and returns the average rating.

4) Build an API that takes a list of email addresses and returns only the valid email IDs.

5)  Define a struct Employee with fields:
   - Name (string)
   - ID (int64)
   - Cadre (string)
   - DOB (string)

   Build an API that takes 10 employees as input and Write a method GetEmployeesDueForRetirement() that lists all the employees who are going to retire in the next two years.

